# 2603
2026 March Memo


## 260305
- Python.h
    - C/C++ 에서 Python 코드를 사용할 때 좋다

    ```cpp
    #include <Python.h>

    int main()
    {
        Py_Initialize();
        PyRun_SimpleString("print('Hello from Python!')");
        Py_Finalize();
    }
    ```

    - 다만, 코드양이 많아지는 경우에는 Pybind 등을 쓰는게 더 나을 듯


## 260313
- VSC에서 C++ 확장을 설치해도 GNU Compiler가 없으면 컴파일이나 디버깅이 안된다

- GNU 용어 정리
    - MinGW: Minimalist GNU for Windows

    - GNU: 자유 소프트웨어로 구성된 완전한 OS 만들기 프로젝트 
        - GNU = GNU's Not Unix

    - GCC: GNU의 Compiler

    - GDB: GNU의 Debugger

    - G++: GCC 안의 C++ 컴파일러
        - 같은 c++ 소스여도 gcc로 컴파일하냐 g++로 컴파일하냐에 따라 
        링킹 결과가 다를 수 있다

    - gnome

- Visual Studio 작업에 따른 프로젝트/솔루션 이름 수정
  - 솔루션은 모르겠는데 프로젝트는 IDE 내에서 이름을 바꿔도 이것이 반영이 안된다

  - 따라서 직접 로컬에서 이름을 바꿔줘야 하는데, 그냥 바꾸기만 하면 오류난다

  - 따라서 sln(솔루션 파일)을 메모장으로 연 뒤 proj 파일명과 디렉토리를 수정하기

  - IDE 내에서도 단순히 프로젝트/솔루션 이름 뿐만 아니라 네임스페이스 이름도 수정!

  - 이렇게 이쁘게 정리를 해줘야 나중에 Readme 쓸 때 편해진다

- \r은 carriage return

- Windows에서의 시스템 콜 호출 구조

    ```markdown
    C/C++ 코드
    ↓
    Windows API
    ↓
    User-mode system library
    ↓
    System Call
    ↓
    Kernel
    ```

  - 우리는 Windows API를 통해 간접적으로 시스템콜을 사용한다
  - 가령, CreateMutex()를 써서 mutex를 사용하는 코드가 MFC 내에서 보인다


## 260316

- Visual Studio에서 변수나 함수가 다른 파일에서 어떻게 쓰이는지 쉽게 추적하려면 
모든 참조 찾기를 활용하자


## Python Interpreter
- Python 인터프리터 동작 구조

    ```markdown
    source.py
    ↓
    bytecode
    ↓
    Python VM
    ↓
    실행
    ```

- Python이 느린 이유
    - 이유1: 파이썬은 컴파일 시 기계어로 변환되지 않는다
        - 대신에 바이트코드를 VM(Virtual Machine)이 해석하는 식으로 동작하므로 느려짐
    
    - 이유2: 파이썬의 자료형은 Dynamic Typing이다
        - VM이 이를 해석하는 과정이 포함되어 더 느리다
  
- Java
  - Java는 인터프리터와 컴파일 언어의 중간 정도
  - JVM, 즉 VM을 거치는 것은 맞지만, 자주 쓰는 코드는 JIT Compile을 통해 기계어로 변환
  - 그렇기에 Python과 C의 중간 정도의 속도를 자랑한다

- 좀 더 정확한 Python 동작 Structure
    
    ```markdown
    Python code
    ↓
    bytecode
    ↓
    Python VM
    ↓
    C 코드
    ↓
    machine code
    ↓
    CPU
    ```


## 260317
- SCP: SSH를 이용한 파일 복사 프로토콜
  
- Git을 통해 Github에 push/pull 해줄 때 HTTPS와 SSH를 사용
  - Git은 파일이 아니라 commit graph와 object를 동기화한다. 
    - 따라서 SCP가 아닌 SSH 프로토콜을 사용하는 것
    
- 쿠버네티스: 많은 수의 도커 컨테이너를 효율적으로 관리해주는 기술
    - 그러나 요즘에는 도커 컨테이너는 지원 안 한다는데 이거 조사하기
  
- Layer4의 QUIC을 이용한 LiDAR Wireless Communication?


## 260318
- NAT vs 브리지 (VM 동작의 관점에서)
  - NAT(Network Address Translation)
    ```markdown
    [VM] → [가상 NAT 라우터] → [Host PC] → [회사 네트워크 스위치] → 인터넷
    ```
  
  - 브리지
    ```markdown
    [VM] → [회사 네트워크 스위치] → 인터넷
    ```

- /dev vs /media vs /mnt
  - /dev: 장치를 파일처럼 다루기 위한 인터페이스 디렉터리
  - /media: 자동 마운트 된 파일 디렉터리
  - /mnt: 수동 마운트 된 파일 디렉터리
 
- 마운트
  - 저장장치를 특정 디렉토리에 연결해서 사용 가능하게 만드는 것
  - 리눅스는 디스크/USB를 바로 못 쓰므로 마운트를 통해 파일 시스템에 연결하여 사용
  - 즉, 마운트는 장치를 디렉토리에 꽂는다 라는 개념
 
- 데몬
  - 백그라운드에서 계속 돌아가는 서비스 프로그램
  - 보통 데몬이 마운트를 자동으로 해주는 경우가 많음
 
- 리눅스 명령어 정리
  - sed(stream editor): 텍스트 파일을 자동으로 수정/치환하는 명령어

  - ip addr: 네트워크 정보 확인할 때 씀

  - dnf: 최신 패키지 관리자 (yum의 업그레이드 버전)

  - rpm: 패키지 파일을 직접 다루는 도구 (파일 직접 설치)

  - systemctl: 서비스(데몬) 관리 명령어
    - 데몬이므로 이 명령어 exec 이후 부팅 시에 자동으로 해당 서비스 ON

  - 고정 IP 설정할 때의 기본 DNS는, 그 이더넷의 DNS를 처리하는 DNS서버의 IP

  - 회사 같은 제한된 네트워크에서는 DHCP를 막고, DNS와 게이트웨이를 지정해줘야 한다. 
    - 따라서 수동 IPv4 지정이 필요함
 

## 260319
- Linux 쓸 때 user별로 디렉터리 환경이나 Config가 다르다 
    - 따라서 Backend 등을 Setup 할 때 이러한 환경 차이를 감안할 것


## 260320
- Visual Studio Code에서 Git-Bash 기본 터미널로
  - `ctrl + ,` 로 설정 들어가서 `terminal.integrated.defaultprofile.windows` 검색
  - null -> git-bash
    
- 마운트 지점 설정 안 하면 재부팅시 마운트 폴더 증발되는 이유
    - 리눅스의 mount는 기본적으로 휘발성(runtime 상태)이다
    - 따라서 재부팅하면 마운트 폴더가 사라지는 것은 당연


## 260321
- README에 상대경로를 지정해서 이미지 넣기

    ```markdown
    <img src="img/scan.png">
    ```


## 260323
- 브리지 vs NAT
  - 브리지
    - VM에 추가적인 IP를 할당하는 것  
    - 즉, VM을 별도의 네트워크 노드로 보는 것

  - NAT: 호스트에 내부망을 만들고 거기에 VM을 연결 (즉, VM을 호스트 안에 숨긴다)
    - 실질적으로 통신은 호스트의 IP가 한다
  
- 전자메일 프로토콜: SMTP/POP3/IMAP
    - SMTP 
        - 클라이언트 to 서버
        - 서버 to 서버  

    - POP3 & IMAP
        - POP3: 메일을 서버에서 가져와서 다운로드
        - IMAP: 메일을 서버에 두고 동기화
        - 클라이언트 to 서버

    - 전달과 관리의 역할이 다르기에 이렇게 프로토콜이 나뉜다


## 260324
- `$ Ansible Playbook` 명령어의 상대경로
    - 그것이 활성화시키는 main.yml의 상대경로는 **명령어**에 따라 달라진다
        - 일반적으로는 main.yml을 기준으로 상대 경로를 적용하면 된다
        - `copy`나 `template`은 ansible playbook 명령어가 실행 디렉터리가 기준
    
- enp0s3나 enp2s0은 장치 위치에 따른 이름
    - s 앞은 PCI 버스 번호, s 뒤는 슬롯 번호
    - 그리고.. 윈도우에서는 이러한 장치명은 안 쓴다
        - 대신 직관적으로 **이더넷**, 이런식으로 표기


## 260325
- SQL 명령어에 문제가 있다면 SQL 서버와 socket 오류가 날 수 있다
    - Query Error시 네트워크 문제로 생각하지 말고 syntax 체크부터!
  
- docker compose는 자동으로 이미지 갱신 X, 명령어에 `build`를 붙여서 갱신해주기
  
- vrrp_instance VI_1 = 하나의 가상 라우터 (= VIP + 상태 관리 단위)
  
- MariaDB의 통신
    - MariaDB에선 localhost != 127.0.0.1
        - 통신 방식이 2가지라서?

    - 통신 방법 1: UNIX Socket
        - host를 안쓰거나 localhost를 쓰면 자동으로 UNIX Socket 통신
        - IP 명시가 안되어 있으니 파일 기반인 UNIX Socket 통신을 한다
        - .sock라는 소켓 파일을 기반으로 커널에서 통신이 발생한다

    - 통신 방법 2: TCP 통신
        - `-h`로 host를 127.0.0.1로 강제하거나, IP주소 사용시 TCP 통신
        - Loopback 네트워크 지정하면 마리아DB에 의해 자동으로 포트가 3306으로 지정

   
## 260326
- 윈도우 자격 증명 때문에 Git이 안되는 경우가 있을 수 있다
  
- VSC 전체 주석은 `ctrl + /`
  
- MariaDB를 도커 환경에서 실행할 경우 권한이나 접근 자체에 문제가 생기는 경우가 많다
    - 도커의 마리아DB 이미지는 `docker-entrypoint.sh`를 실행하며 시작
    - 따라서 권한이나 접근에 문제가 생긴다면 dive 등을 통해 entrypoint 체크!
  
- Virtual Machine에서 NAT 네트워크와 DHCP 관계 알아두기


## 260327
- VM에서 NAT을 쓰는 경우
  - 호스트 컴퓨터가 NAT 게이트웨이 역할을 하며, 가상 머신의 네트워크 트래픽을 인터넷으로 라우팅한다.
  - VirtualBox NAT 엔진이 DHCP로 강제로 IP를 지정 (10.0.2.15 부터 자동~)
    - 그래서 만약 고정 IP를 사용하면 변환 과정에서 네트워크 내 기기로 판단하지 않고 먹통이 되는 것..!
      
- LAN에서의 IP vs MAC
  - LAN에서 실제 데이터의 전달은 MAC 주소를 통해서 이루어진다
  - 다만.. 송신지와 수신지를 특정하는 논리적 체계로서.. 페이로드에 있는 IP 주소 정보를 활용한다
  - 아 그리고 이때 ARP Protocol을 썼었지!
    
- SHEBANG
  - 대표적인 쉬뱅 구문
  ```
  #!      → shebang (이 파일은 스크립트다)
  /bin/bash  → 이 파일을 bash 로 해석해라
  ```
  - 즉 파일이 바이너리인지 스크립트인지 명확하게 정해준다
 

## 260328
- 사설 IP 정리

    ```markdown
    A클래스: 10.0.0.0/8 (엄청 큼)
    B클래스: 172.16.0.0/12 (중간)
    C클래스: 192.168.0.0/16 (작음, 가정용) 
    ```


## 260329
- PIP(Package Installer for Python) 관련 이슈
    - Python의 PATH를 아나콘다보다 우선으로 놔도, pip와는 별개이다
    - pip의 PATH도 아나콘다보다 우선으로 놔야 Python 본진에서 패키지가 설치


## 260330
- Docker의 경우 컨테이너가 종료되면 작업 내용이 날아갈 수 있다
    - volume을 통한 디렉터리 마운트로 작업 내용을 호스트PC에 저장하기!
